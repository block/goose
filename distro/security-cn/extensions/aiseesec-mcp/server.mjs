#!/usr/bin/env node

console.error(
  "[aiseesec-mcp] Goal 5 preview stub. This catalog entry is wired for local preview only and does not implement the live AiseeSec MCP integration yet."
);
process.exit(1);
