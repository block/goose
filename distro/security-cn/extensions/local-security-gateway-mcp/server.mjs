#!/usr/bin/env node

console.error(
  "[local-security-gateway-mcp] Goal 5 preview stub. This catalog entry is wired for local preview only and does not implement the live security gateway integration yet."
);
process.exit(1);
