/**
 * Re-export the official ACP SDK
 * 
 * This module used to contain a hand-rolled ACP client implementation.
 * Now we use the official @agentclientprotocol/sdk package instead.
 */

export * from "@agentclientprotocol/sdk";
